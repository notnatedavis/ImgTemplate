//   js/main.js
//   App entry point – imports and initialises everything

// ----- Imports -----
// (uiController.js handles all DOM wiring; just import it to execute)
import './uiController.js';

// setup done inside uiController.js when the module loads
// file exists purely as the main entry point for the Vite bundler